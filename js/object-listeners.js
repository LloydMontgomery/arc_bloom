var transition = Kinetic.Easings.StrongEaseOut

// var tween = new Kinetic.Tween({
// 	node: scope.arcs[0],
// 	duration: 1,
// 	rotation: 120,
// 	easing: transition
// });

// var tween3 = new Kinetic.Tween({
// 	node: scope.arcs[0],
// 	duration: 1,
// 	scaleX: 1.34,
// 	scaleY: 1.34,
// 	//strokeWidth: scope.arcs[0].attrs.strokeWidth/1.5,
// 	easing: transition
// });

function activate() {
	console.log("Activate")

	// Highlight the currently selected pieces
	// for (var i = 0; i < scope.arcPos[0].length; i++) {
	// 	if (scope.arcPos[0][i] != null) {
	// 		console.log(scope.arcs[scope.arcPos[0][i]]);
	// 		scope.arcs[scope.arcPos[0][i]].stroke('black');
	// 	};
		
	// };
	//scope.playLayer.draw();
	//console.log(tween.node.attrs.rotation)
	//tween3.play();

	// new Kinetic.Tween({
	// 	node: scope.arcs[0],
	// 	duration: 1,
	// 	scaleX: 1.429*scope.arcs[0].attrs.scaleX,
	// 	scaleY: 1.429*scope.arcs[0].attrs.scaleY,
	// 	strokeWidth: scope.arcs[0].attrs.strokeWidth/1.429,
	// 	easing: transition
	// }).play();
	//tween2.play();

	// setTimeout(function(){ 
	// 	//console.log(tween.node.attrs.rotation)
	// 	tween3.play();

	// }, 1000);

	// setTimeout(function(){
	// 	tween3.destroy();
	// }, 3000);
	
}
